<link rel="stylesheet" href="/css/cards.css">
<div class="col-md-12" style="margin-bottom: 5px; ">
    <div class="card text-center border border-primary">
        <h3 class="card-title text-uppercase" style="color: rgb(44, 142, 253);">
            Lista de dulces
        </h3>
    </div>
</div>
<a class="color" href="/dulces/add">Añadir equipo </a>
{{#each dulces}}
<div class="card-body">
    <p>{{nombre_dulce}}</p>
    <p>{{descripcion}}</p>
     {{#if foto}}
    <img class="card__image" src="/{{foto}}" loading="lazy" alt="User Profile">
    {{else}}
    <img src="/not-found/not-found-image.png" class="card__image" alt="..." alt="User Profile">
    {{/if}}
    <p>{{precio}}</p>
    
    <center>
        <a href="/dulces/delete-dulces/{{id}}" class="danger">Eliminar</a>
        <a href="/dulces/edit-dulces/{{id}}" class="secondary">Editar</a>
        <form action="/teams/foto-dulce/{{id}}" method="POST" encType="multipart/form-data">
            <input type="file" name="sampleFile" accept="image/*" />
            <input type="submit">
        </form>
    </center>
</div>
{{else}}
<center> <p>No hay registro guardados a un </p>
    <a href="/dulces/add"> Agrega uno haciendo click aqui</a>
</center>
{{/each}}


